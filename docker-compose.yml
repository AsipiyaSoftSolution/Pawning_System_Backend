services:
  pawning_backend:
    build: .
    container_name: pawning_backend
    restart: always
    environment:
      PORT: "5000"
    env_file:
      - .env
    ports:
      - "127.0.0.1:5000:5000"
    networks:
      - tool_net

networks:
  tool_net:
    external: true

